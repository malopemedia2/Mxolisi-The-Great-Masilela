{
  "name": "mxolisi-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
  "build": "npx vite build", 
  "preview": "npx vite preview",
  "postinstall": "chmod +x node_modules/.bin/vite"
  }
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^5.0.0"
  }
}
